@model IEnumerable<DotNetAppSqlDb.Models.Passeio>
@{
    ViewBag.Title = "PasseiosAluno";
    Layout = "~/Views/Shared/AlunoView.cshtml";
    DotNetAppSqlDb.Models.Aluno alunoModel = (DotNetAppSqlDb.Models.Aluno)ViewData["AlunoModel"];
}

<html>
<head>

</head>
<body>
    <div class="col-lg-12">
        <div class="well">
            <a style="text-align:center">@TempData["Mensagem"]</a>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayName("Nome da Empresa")
                    </th>
                    <th>
                        @Html.DisplayName("Data do Passeio")
                    </th>
                    @*<th>
                    @Html.DisplayName("Sua Autorização")
                </th>*@
                </tr>


                @foreach (var item in Model)
        {
            @Html.HiddenFor(model => item.IdPasseio)
            <tr>
                <td>
                    @Html.DisplayFor(model => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Data)
                </td>
                @*<td>
                @Html.DisplayFor(model => item.Autorizacao.URL)
            </td>*@
                @*<td>
                @Html.ActionLink("Detalhes", "VerificaAutorizacao", "Resultado", new { idPasseio = item.IdPasseio, idAluno = resultado.IdAluno }, new { @class = "btn btn-success" })
            </td>*@

            </tr>
}

            </table>
            <div class="col-md-3">
                @Html.ActionLink("Voltar", "IndexAluno", "Aluno", alunoModel, new { @class = "btn btn-primary" })
            </div>
        </div>
    </div>
</body>
</html>
